Precisa do compilador valac.

== Compilacao ==

Va para dentro da pasta do projeto

+ Crie o diretorio que o compilador vai trabalhar
    slingscold $ mkdir build
    slingscold $ cd build

+ Inicia a CMAKE:
    slingscold/build $ cmake ..

    resolva as dependencias, serao libs tipo dev.

- Compilar:
    slingscold/build $ make -j3



== Executar ==

- Apenas digite:
    slingscold/build $ ./slingscold



== Instalando Slingscold ==

- Com Permissao de ADM, digite:

    slingscold/build $ sudo make install

    ou

    slingscold/build # make install
